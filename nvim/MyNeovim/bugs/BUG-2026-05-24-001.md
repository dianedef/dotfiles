# BUG-2026-05-24-001

- Title: Gemini CLI terminal exits with code 127
- Status: fixed-pending-retest
- Severity: medium
- First seen: 2026-05-24
- Last tested: 2026-05-24
- Next step: redemarrer Neovim, puis retester `Space a g t`

## Story

- Actor: utilisateur Neovim du depot
- Trigger: lancement de Gemini CLI depuis les mappings `<leader>ag*`
- Observed: le terminal Gemini quitte avec `Terminal exited with code 127`; les actions fichier/diagnostic affichent aussi `No valid file in current buffer` ou `No diagnostics found in the current buffer` selon le buffer courant
- Expected: le terminal Gemini s'ouvre si le CLI est installe; les messages fichier/diagnostic doivent etre compris comme des preconditions locales du buffer

## Reproduction

- Preconditions:
  - `gemini` absent du `PATH` visible par Neovim
- Steps:
  1. Ouvrir Neovim avec cette configuration.
  2. Lancer le mapping Gemini terminal.
  3. Observer la sortie du terminal integre.

## Diagnosis Notes

- 2026-05-24 17:43:39 UTC: `command -v gemini` ne retournait rien avant installation.
- 2026-05-24 17:43:39 UTC: `npm list -g --depth=0 @google/gemini-cli` montrait que le package n'etait pas installe dans `/home/claude/.npm-global`.
- 2026-05-24 17:43:39 UTC: le plugin utilise `gemini_cmd = "gemini"` par defaut, donc un binaire absent produit le code shell `127`.
- 2026-05-24 17:43:39 UTC: les messages `No diagnostics found in the current buffer` et `No valid file in current buffer` viennent des preconditions du plugin, pas d'un crash Gemini.

## Fix Attempts

- 2026-05-24 17:43:39 UTC
  - Files changed: `lua/plugins/gemini-cli.lua`, `Gemini CLI.md`, `AI Plugins.md`
  - Environment changed: `npm install -g @google/gemini-cli`
  - Hypothesis: installer le binaire `gemini` corrige le code `127`; un garde-fou de mapping evite une erreur obscure sur les machines ou le CLI manque.
  - Validation: `command -v gemini`, `gemini --version`, `npm list -g --depth=0 @google/gemini-cli`, `nvim --headless "+lua print(vim.fn.executable('gemini'))" "+qa"`, `:checkhealth gemini_cli` apres chargement du plugin.
  - Result: fixed-pending-retest; Gemini CLI detecte en version `0.42.0` et healthcheck vert.

## Retest History

- 2026-05-24 17:43:39 UTC
  - Command: `nvim --headless "+lua require('gemini_cli').setup({})" "+checkhealth gemini_cli" "+write! /tmp/gemini-cli-health.txt" "+qa!"`
  - Environment: local Neovim headless
  - Result: passed
  - Notes: healthcheck Gemini vert; `gemini v0.42.0 found`, `snacks.nvim plugin found`.

## Closure Criteria

- Redemarrer Neovim pour repartir avec le PATH et les mappings charges.
- Retester `Space a g t` dans un buffer normal.
- Pour `Space a g a`, etre dans un fichier sauvegarde.
- Pour `Space a g d`, etre dans un buffer qui contient des diagnostics LSP.
