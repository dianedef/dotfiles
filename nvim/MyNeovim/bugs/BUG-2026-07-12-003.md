# BUG-2026-07-12-003

- Title: Mail Intelligence handoff does not reach the terminal clipboard
- Status: fixed-pending-verify
- Severity: medium
- First seen: 2026-07-12
- Last tested: 2026-07-12
- Next step: retain the fix pending the broader v2 verification

## Story

- Actor: utilisateur Neovim qui route une fiche Mail Intelligence
- Trigger: appuyer sur `h` dans la liste des propositions
- Observed: notification `End of copy`, mais aucun contenu disponible dans le presse-papiers système
- Expected: le handoff `#source` est disponible dans le presse-papiers système et dans le registre Neovim

## Diagnosis

- Le handoff appelait uniquement `vim.fn.setreg("+", ...)`.
- La configuration locale utilise déjà un helper OSC 52, avec un fichier de secours `/tmp/nvim_notif.txt`, pour copier les notifications.

## Fix Attempts

- 2026-07-12 21:47:16 UTC
  - Files changed: `lua/shipglows/clipboard.lua`, `lua/config/keymaps.lua`, `lua/shipglows/mail/review.lua`, `lua/shipglows/mail/reader.lua`
  - Fix: centraliser la copie via le registre Neovim, OSC 52 et `/tmp/nvim_notif.txt`.
  - Safety: le contenu copié par `h` reste limité aux métadonnées de handoff; aucun corps d’email n’est ajouté.
  - Retest: pending.

## Evidence

- User-visible notification: `End of copy`.
- User reported the system clipboard remained empty.

## Retest History

- 2026-07-12 22:42:46 UTC
  - Scope: real migrated private Maildir source in Neovim after restart.
  - Steps: opened the review queue, focused the list, invoked `h`, then inspected `:reg +`.
  - Result: pass; the governed `#source` handoff was copied with routing metadata and no email body.
  - Residual: system-terminal clipboard transport is covered by the shared OSC 52 path; the register also contains the verified value.
