# BUG-2026-07-12-001

- Title: Mail Intelligence v2 Avante handoff omits governed queue fields
- Status: retested-bounded
- Severity: medium
- First seen: 2026-07-12
- Last tested: 2026-07-12 15:53:15 UTC
- Next step: retain the v2 chantier as partial; Task 4 classification and Task 7 scheduling remain pending

## Story

- Actor: utilisateur Neovim qui examine une fiche Mail Intelligence
- Trigger: demander l'analyse Avante ou copier le handoff `#source`
- Observed: l'appel Avante utilisait l'option `ask`, ignorée par l'API locale qui attend `question`; le handoff ne transmettait que `owner=unknown`, `output`, et `source_id`
- Expected: Avante reçoit la question attendue; le handoff reprend les champs de routage connus de la fiche sans copier le corps de l'email et conserve l'approbation explicite de l'opératrice

## Diagnosis

- La version locale de `avante/api.lua` vérifie `opts.question`; `review.lua` envoyait `opts.ask`.
- Les fiches de queue produisent un frontmatter structuré avec `project`, `angle`, `owner_skill`, `confidence`, `risk`, `status` et `source_id`, mais `handoff()` ne le lisait pas.
- Aucun accès à `/home/claude/Mail`, à Gmail, à notmuch ou à la queue privée n'a été utilisé pour diagnostiquer ou valider ce correctif.

## Fix Attempts

- 2026-07-12 15:49:49 UTC
  - Files changed: `lua/shipglowz/mail/review.lua`
  - Hypothesis: utiliser `question` et extraire uniquement le frontmatter de la fiche courante rétablit le contrat Avante et le handoff gouverné.
  - Fix: remplacement de l'option Avante; parseur frontmatter borné; handoff enrichi avec `project`, `angle`, `owner_skill`, `suggested_action`, `confidence`, `risks`, `status` et `source_id`, avec `unknown` pour les champs absents.
  - Safety: aucun corps d'email n'est lu par le handoff; aucune création de contenu, publication ou envoi automatique n'est ajouté.
  - Retest command: `nvim -u NONE --headless` avec runtimepath local, fixture de queue temporaire, vérification de l'enregistrement des commandes et du registre `#source`.
  - Result: fix-attempted; retest borné à compléter.

## Retest History

- 2026-07-12 15:53:15 UTC
  - Scope: bounded `/103-sg-verify` retest with a sanitized temporary queue fixture outside the repository and private root.
  - Evidence: all 10 user commands registered; `<leader>mi` and `<leader>ms` target `MailIntake` and `MailIntakeScan`; handoff contains project, angle, owner skill, suggested action, confidence, risks, status, and source ID; fixture body sentinel is absent; installed Avante API and source both use `question`; unavailable-Avante fallback completes; both CLI help surfaces, `git diff --check`, and duplicate-wrapper checks pass.
  - Result: bounded retest passed; bug behavior is verified locally within scope.
  - Limits: no real Avante provider call and no notmuch/Maildir or private queue access was performed.

## Residual Limits

- La validation locale ne prouve pas l'interaction réelle avec un provider Avante ni l'ouverture d'un message réel via notmuch/Maildir.
- La validation ne couvre pas Task 4 (classifieur) ni Task 7 (scheduling), volontairement hors périmètre.

## Closure Criteria

- Rejouer le fixture headless avec un frontmatter connu et vérifier toutes les lignes du handoff.
- Vérifier l'absence d'exception quand Avante est indisponible.
- Repasser `/103-sg-verify` avant toute clôture.
