# BUG-2026-07-12-002

- Title: Mail Intelligence reopens an already named source buffer and raises E95
- Status: fixed-pending-verify
- Severity: medium
- First seen: 2026-07-12
- Last tested: 2026-07-12
- Next step: retain the fix pending the broader v2 verification

## Story

- Actor: utilisateur Neovim qui demande l'analyse IA d'une fiche déjà ouverte
- Trigger: ouvrir une fiche avec `<CR>`, puis revenir à la liste et appuyer sur `a`
- Observed: Neovim raises `E95: Buffer with this name already exists` at `mail/review.lua:127`
- Expected: l'analyse IA s'ouvre sans exception, même si la source est déjà visible dans une autre fenêtre

## Fix Attempts

- 2026-07-12
  - Files changed: `lua/shipglows/mail/review.lua`
  - Fix: source buffers now receive a unique fallback name when the canonical email buffer name is already present.
  - Retest: pending operator retest with the real migrated Maildir source.

## Evidence

- User-visible Neovim error:
  `Error executing vim.schedule lua callback ... E95: Buffer with this name already exists`
- No raw email content stored in this bug record.

## Retest History

- 2026-07-12 21:43:58 UTC
  - Scope: real migrated private Maildir source in Neovim.
  - Steps: reopened the queue item with `<CR>`, returned to the list, then invoked `a`.
  - Result: pass; Avante opened with the expected analysis pre-prompt and no E95 buffer-name exception.
  - Residual: provider completion itself was not required for this buffer-lifecycle fix.
